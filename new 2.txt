import os

# Define your base folder and output names ONCE here
base_folder = r"C:\Users\TAQ7353\UTR\LOB_ActivitySummary_FillingRate\test"
output_file_name = "SummaryOfActivity_Results.xlsx"

# Export them as environment variables so other .py scripts can access them
os.environ["PDF_FOLDER"] = base_folder
os.environ["OUTPUT_FILE"] = output_file_name

print("Environment variables set successfully ✅")


####################################################


import os

# Read paths that were set from the notebook
pdf_folder = os.environ.get("PDF_FOLDER", r".")  # Default '.' if not set
output_file = os.environ.get("OUTPUT_FILE", "default_output.xlsx")

print(f"Using folder: {pdf_folder}")
print(f"Saving to: {output_file}")
